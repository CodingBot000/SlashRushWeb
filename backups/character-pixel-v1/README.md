# Character Pixel Backup v1

This folder preserves the pixel-style main-character sprites before the active Phaser player mapping was restored to the original Godot character images.

- The files here are backups only and are not deleted from `public/assets/generated/player-pixel-preview`.
- The active player sprites now load from `public/assets/godot-source/assets`.
- Enemy pixel assets and the rest of the Phaser game remain unchanged.
